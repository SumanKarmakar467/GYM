Place Mixamo-exported GLB workout clips in this folder.

Expected filenames:

- squat.glb
- push-up.glb
- pull-up.glb
- jumping-jacks.glb
- workout.glb

The workout page automatically maps exercises to these files. If a file is missing, the app shows the built-in animated 3D fallback human instead.
